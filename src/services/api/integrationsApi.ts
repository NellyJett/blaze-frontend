// import { integrationApi } from '@/services/api';
// // or relative:
// // import { integrationApi } from '../services/api';


// export const getIntegrations = async () => {
//   const res = await api.get('/integrations');
//   return res.data.data;
// };

// export const configureIntegration = async (
//   provider: string,
//   config: Record<string, any>
// ) => {
//   const res = await api.post(`/integrations/${provider}/configure`, config);
//   return res.data.data;
// };

// export const syncIntegration = async (provider: string) => {
//   const res = await api.post(`/integrations/${provider}/sync`);
//   return res.data.data;
// };
